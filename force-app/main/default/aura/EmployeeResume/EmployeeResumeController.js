({
    doInit : function(component, event, helper) {
       /* var funcId = component.get("v.recordId");
        var urlEvent = $A.get("e.force:navigateToURL");
        urlEvent.setParams({
            "url": "/lightning/r/" + funcId + "/related/Ferias__r/view"
            
        });
        urlEvent.fire(); */
    } 
})